# =====================================================
# MINECRAFT SERVER MANAGER - CONFIGURATION
# =====================================================
# 
# Fill in your Proxmox server details below.
# This file contains sensitive information - DO NOT commit to git!
# 
# How to find these values:
# - PROXMOX_HOST: IP or hostname of your Proxmox server
#   Example: 192.168.1.100 or proxmox.local
#
# - PROXMOX_USERNAME: Your Proxmox login username
#   Can be: root, admin, or a custom user like apimanager
#   Full format is usually: username@realm
#   Example: root@pam or apimanager@pam
#
# - PROXMOX_PASSWORD: Your Proxmox login password
#   This is your Proxmox admin/user password
#
# - PROXMOX_REALM: Your Proxmox realm (usually 'pam')
#   PAM = Proxmox Authentication Module (local)
#   Could also be: ldap, pve, or custom realm
#

# =====================================================
# REQUIRED - Fill these in!
# =====================================================

# Your Proxmox server IP or hostname
# Example: 192.168.1.100
# Example: proxmox.example.com
PROXMOX_HOST=

# Your Proxmox username (can be root or custom user)
# Example: root
# Example: apimanager
# Full format in Proxmox: username@realm
PROXMOX_USERNAME=

# Your Proxmox password
# Use a strong password or API token for security
PROXMOX_PASSWORD=

# Your Proxmox realm (default is 'pam' for local auth)
# Usually 'pam' unless you use LDAP or custom auth
PROXMOX_REALM=pam

# =====================================================
# OPTIONAL - Usually fine as-is
# =====================================================

# Backend API port (default: 5000)
PORT=5000

# Node environment (production or development)
NODE_ENV=production

# Frontend API URL (for Docker, leave as-is)
# This is automatically handled in docker-compose.yml
# For development, change to: http://localhost:5000
REACT_APP_API_URL=http://backend:5000

# =====================================================
# EXAMPLES - Replace with your actual values:
# =====================================================
# 
# Example for root user (default):
#   PROXMOX_HOST=192.168.1.100
#   PROXMOX_USERNAME=root
#   PROXMOX_PASSWORD=yourRootPassword
#   PROXMOX_REALM=pam
#
# Example for custom API user (recommended):
#   PROXMOX_HOST=proxmox.local
#   PROXMOX_USERNAME=apimanager
#   PROXMOX_PASSWORD=apiPassword123
#   PROXMOX_REALM=pam
#
# =====================================================

# =====================================================
# SECURITY WARNING
# =====================================================
#
# ⚠️  IMPORTANT SECURITY NOTES:
#
# 1. This file contains sensitive credentials
#    - Add .env to .gitignore (already done)
#    - Never commit this file to git
#    - Never share this file publicly
#
# 2. For production deployment:
#    - Create a dedicated Proxmox user with minimal permissions
#    - Don't use root credentials
#    - Rotate passwords regularly
#    - Consider using API tokens instead of passwords
#
# 3. Network security:
#    - Keep the app on internal network
#    - Use HTTPS/SSL in production
#    - Implement authentication for the web interface
#    - Consider running behind a VPN
#
# =====================================================

# =====================================================
# PROXMOX USER CREATION (Optional but Recommended)
# =====================================================
#
# For security, create a dedicated API user:
#
# SSH to Proxmox and run:
#   pveum useradd apimanager@pam -password 'secure-password'
#   pveum roleadd MinecraftManager -privs "VM.Allocate,VM.Clone,VM.PowerMgmt,VM.Monitor"
#   pveum aclmod / -user apimanager@pam -role MinecraftManager
#
# Then use:
#   PROXMOX_USERNAME=apimanager
#   PROXMOX_PASSWORD=secure-password
#
# =====================================================
