# Proxmox Node Configuration
# List the Proxmox nodes to monitor (comma-separated)
PROXMOX_NODES=pve

# Default VM Settings for Cloning
# These are optional and override Proxmox defaults if set
DEFAULT_CPU_CORES=2
DEFAULT_MEMORY_MB=4096
DEFAULT_DISK_GB=20
